[build]
  command = "npm run build"
  publish = "dist"

# Vite 7 krever Node ^20.19 || >=22.12. Pinnes eksplisitt så builden ikke
# faller tilbake på en eldre Netlify-default.
[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
